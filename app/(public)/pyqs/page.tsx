import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export const metadata = { title: "PYQ Vault" };

export default function PyqsPage() {
  return (
    <PagePlaceholder
      title="PYQ Vault"
      note="Previous year questions with filters for stage, year, micro-theme and tag — plus Mains model answers — arrive in Phase 5."
    />
  );
}
