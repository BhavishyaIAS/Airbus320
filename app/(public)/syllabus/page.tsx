import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export const metadata = { title: "Syllabus" };

export default function SyllabusPage() {
  return (
    <PagePlaceholder
      title="Syllabus tree"
      note="The browsable Subject → Topic → Micro-theme tree, showing which micro-themes have published notes, arrives in Phase 3."
    />
  );
}
